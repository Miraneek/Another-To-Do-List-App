import {RegisterFrom} from "@/modules/auth/RegisterForm";

export default function page() {

    return <main className={"min-h-screen flex flex-col items-center justify-center"}>
        <RegisterFrom/>
    </main>
}