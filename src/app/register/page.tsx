import {RegisterFrom} from "@/modules/auth/RegisterForm";

export default function page() {

    return <main className={"min-h-full h-[90vh] flex flex-col items-center justify-center"}>
        <RegisterFrom/>
    </main>
}